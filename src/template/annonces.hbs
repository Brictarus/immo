<div class="annonces-list">
  {{#each collection}}
    <div class="row">
      <div class="col-md-12">
        <div class="col-md-2 col-xs-4">
          <div class="thumbnail-annonce">
            <a href="{{../config.urlRoot}}/#annonce/{{this.id}}">
              {{#if this.photo_favorite}}
                <img class="img-thumbnail" src="data/thumbnails/{{this.photo_favorite.id}}.{{this.photo_favorite.extension}}" alt="">
              {{else}}
                <img class="img-thumbnail" src="favicon.png" alt="">
              {{/if}}
            </a>
            <div class="pics-nb">{{this.nb_photos}}</div>
          </div>
        </div>
        <div class="col-md-10 col-xs-8">
          <div class="row title">
            <a href="{{../config.urlRoot}}/#annonce/{{this.id}}">{{this.label}}</a>
          </div>
          <div class="row creation-date">Ajoutée le {{formatDate this.date_creation "DD/MM/YYYY"}}
            à {{formatDate this.date_creation "HH:mm"}}</div>
          {{#if this.adresse}}<div class="row city">{{this.adresse}}</div>{{/if}}
          {{#if this.prix}}<div class="row price">{{this.prix}} €</div>{{/if}}
        </div>
      </div>
    </div>
    {{#unlessequals @index ../lastIndex}}
      <hr>{{/unlessequals}}
  {{else}}
    Aucune annonce présente
  {{/each}}
</div>
